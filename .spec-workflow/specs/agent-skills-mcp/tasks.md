# Tasks Document

- [x] 1. Establish extension runtime contracts and MCP SDK dependency
  - Files: `package.json`, `src/agent-extension-types.ts`, `test/agent-extension-types.test.ts`
  - Add the official `@modelcontextprotocol/sdk` dependency and define shared `ExtensionDiagnostic`, Skill source, MCP source, status, and runtime callback contracts.
  - Keep contract types independent of TUI and Pi Agent implementation details.
  - _Leverage: `src/agent-controller.ts`, `src/commands.ts`, `@oh-my-pi/pi-agent-core` `AgentTool` types_
  - _Requirements: 1.1–1.6, 2.1–2.5, 3.1–3.6, 4.1–4.4_
  - _Prompt: Implement the task for spec agent-skills-mcp, first run spec-workflow-guide to get the workflow guide then implement the task: Role: TypeScript platform engineer | Task: Add the MCP SDK and shared extension contracts required by Skill and MCP runtime modules | Restrictions: Use Bun, preserve existing Agent tool contracts, add behavior tests before implementation, do not create an OAuth broker, do not exceed 250 non-blank lines per source file | _Leverage: src/agent-controller.ts, src/commands.ts, @oh-my-pi/pi-agent-core AgentTool | _Requirements: 1.1–1.6, 2.1–2.5, 3.1–3.6, 4.1–4.4 | Success: Dependency resolves, contracts compile, source metadata and status types support all requirement states, tests cover contract-visible invariants | Before starting mark this task [-] in tasks.md; after implementation log artifacts with spec-workflow log-implementation; then mark this task [x] in tasks.md._

- [x] 2. Implement OMP-compatible Skill discovery and safe URI resolution
  - Files: `src/skills.ts`, `test/skills.test.ts`
  - Discover the approved OMP-compatible project/user roots non-recursively, parse supported frontmatter, resolve collisions deterministically, produce diagnostics, and read bounded `skill://` resources.
  - _Leverage: OMP `skills.md` precedence and URI boundary rules; `src/width.ts` only for consumers, not registry I/O_
  - _Requirements: 1.1–1.4, 1.6, 4.2_
  - _Prompt: Implement the task for spec agent-skills-mcp, first run spec-workflow-guide to get the workflow guide then implement the task: Role: TypeScript filesystem-security engineer | Task: Build the SkillRegistry with OMP-compatible one-level discovery, frontmatter metadata, first-wins conflict handling, diagnostics, and skill:// path containment | Restrictions: Never recursively scan roots, reject absolute and traversal paths before reads, isolate malformed skills, use deterministic roots injected for tests, no generic filesystem read capability | _Leverage: OMP skills.md and src/width.ts conventions | _Requirements: 1.1–1.4, 1.6, 4.2 | Success: Tests cover all source priorities, hidden/model flags, malformed files, duplicates, and URI traversal/escape failures | Before starting mark this task [-] in tasks.md; after implementation log artifacts with spec-workflow log-implementation; then mark this task [x] in tasks.md._

- [x] 3. Expose Skill metadata, restricted read tool, and dynamic skill commands
  - Files: `src/skill-tool.ts`, `src/commands.ts`, `src/command-prompt.ts`
  - Convert SkillRegistry results into a `read` AgentTool restricted to `skill://`; render prompt supplements; support dynamic `/skill:<name> [args]` dispatch and completion without exposing hidden or model-disabled Skills.
  - _Leverage: `src/agent-tools.ts`, `src/command-prompt.ts`, `src/commands.ts`, `@oh-my-pi/pi-agent-core` JSON-schema tools_
  - _Requirements: 1.3–1.5, 4.1, 4.4_
  - _Prompt: Implement the task for spec agent-skills-mcp, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Agent interaction developer | Task: Add a restricted OMP-style Skill read tool and dynamic /skill:name command/completion integration | Restrictions: The read tool must accept only skill:// paths; no generic file reads; preserve existing command parsing and width behavior; Skill body is injected only for explicit invocation or alwaysApply metadata | _Leverage: src/agent-tools.ts, src/commands.ts, src/command-prompt.ts | _Requirements: 1.3–1.5, 4.1, 4.4 | Success: Tests prove prompt metadata filtering, bounded reads, explicit skill invocation, dynamic suggestions, and unknown/hidden errors | Before starting mark this task [-] in tasks.md; after implementation log artifacts with spec-workflow log-implementation; then mark this task [x] in tasks.md._

- [x] 4. Implement OMP-compatible MCP config discovery and validation
  - Files: `src/mcp/config.ts`, `test/mcp-config.test.ts`
  - Load and validate `.omp/mcp.json`, `mcp.json`, `.mcp.json`, and `~/.omp/agent/mcp.json`; apply source precedence, user `disabledServers`, `${VAR}` expansion, field validation, and secret-safe diagnostics.
  - _Leverage: OMP `mcp-config.md`, `src/json-file.ts`, Bun URL and filesystem APIs_
  - _Requirements: 2.1–2.5, 4.2–4.3_
  - _Prompt: Implement the task for spec agent-skills-mcp, first run spec-workflow-guide to get the workflow guide then implement the task: Role: TypeScript configuration and security engineer | Task: Implement OMP-shaped MCP configuration discovery, expansion, precedence, disabled server filtering, and strict transport validation | Restrictions: Support only stdio/http/sse; HTTP URLs must be HTTP(S); unresolved variables remain literal; never execute ! shell substitutions; never include resolved secrets in diagnostics | _Leverage: OMP mcp-config.md, src/json-file.ts | _Requirements: 2.1–2.5, 4.2–4.3 | Success: Tests cover configuration source priority, duplicate server names, disabledServers, enabled false, expansion defaults/literals, invalid names/types/fields, and redaction | Before starting mark this task [-] in tasks.md; after implementation log artifacts with spec-workflow log-implementation; then mark this task [x] in tasks.md._

- [x] 5. Implement MCP connection manager with stdio, HTTP, SSE lifecycle
  - Files: `src/mcp/manager.ts`, `test/mcp-manager.test.ts`
  - Use the official MCP SDK to construct transports, initialize clients, list tools, track state, isolate failures, reload, reconnect with bounded backoff, and dispose resources.
  - _Leverage: OMP `mcp-protocol-transports.md`, `mcp-runtime-lifecycle.md`, `AbortSignal.timeout`, Bun test fixture servers_
  - _Requirements: 3.1, 3.2, 3.4–3.6, 4.3–4.4_
  - _Prompt: Implement the task for spec agent-skills-mcp, first run spec-workflow-guide to get the workflow guide then implement the task: Role: MCP runtime engineer | Task: Build the MCPManager using the official SDK for stdio, Streamable HTTP, and legacy SSE connections with deterministic state, reload, reconnect and disposal | Restrictions: Do not hand-roll JSON-RPC; connect servers independently; never block the caller on one failed server; cancel backoff timers and close every transport on reload/dispose; cap retries at OMP-compatible backoff/circuit-breaker limits | _Leverage: OMP mcp-protocol-transports.md and mcp-runtime-lifecycle.md | _Requirements: 3.1, 3.2, 3.4–3.6, 4.3–4.4 | Success: Deterministic fixture tests cover initialize/list, per-server failure isolation, timeout, reconnect, reload, and disposal without orphan child processes | Before starting mark this task [-] in tasks.md; after implementation log artifacts with spec-workflow log-implementation; then mark this task [x] in tasks.md._

- [x] 6. Bridge MCP tool schemas and results into Pi Agent tools
  - Files: `src/mcp/tool-bridge.ts`, `test/mcp-tool-bridge.test.ts`
  - Convert remote tool definitions to namespaced `mcp__<server>_<tool>` AgentTools, preserve JSON Schema and descriptions, call remote tools, and map text/image/error results safely.
  - _Leverage: `@oh-my-pi/pi-ai` `TJsonSchema`, `@oh-my-pi/pi-agent-core` `AgentTool`, `src/agent-tools.ts` result conventions_
  - _Requirements: 3.3–3.4, 4.1_
  - _Prompt: Implement the task for spec agent-skills-mcp, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Agent tool integration engineer | Task: Bridge MCP tools to Pi AgentTools with stable sanitized names, JSON Schema preservation, and bounded error/result conversion | Restrictions: Do not mutate local tool definitions, keep remote schemas as JSON Schema, preserve server namespace, return tool-level errors instead of throwing session-wide failures, include no secrets in errors | _Leverage: @oh-my-pi/pi-ai TJsonSchema, @oh-my-pi/pi-agent-core AgentTool, src/agent-tools.ts | _Requirements: 3.3–3.4, 4.1 | Success: Tests cover name collisions/sanitization, schema forwarding, remote call arguments, text/image content, malformed results, and remote error isolation | Before starting mark this task [-] in tasks.md; after implementation log artifacts with spec-workflow log-implementation; then mark this task [x] in tasks.md._

- [x] 7. Integrate extension runtime with Pi Agent, app lifecycle, and MCP commands
  - Files: `src/agent-extensions.ts`, `src/pi-agent.ts`, `src/app.ts`
  - Combine base/local/Skill/MCP tools, update Agent tools and system prompt dynamically, wire `/mcp list|reload|reconnect`, invoke Agent redraw on extension state changes, and deterministically dispose MCP on app quit.
  - _Leverage: `src/pi-agent.ts`, `src/app.ts`, `src/main.ts`, `src/commands.ts`, `Agent.setTools()`_
  - _Requirements: 1.4–1.5, 2.1–2.5, 3.3–3.6, 4.1–4.4_
  - _Prompt: Implement the task for spec agent-skills-mcp, first run spec-workflow-guide to get the workflow guide then implement the task: Role: TUI Agent runtime integrator | Task: Wire AgentExtensionRuntime into the existing PiAgentDriver and MarketIntelligenceApp so Skills and MCP tools update live, MCP commands report bounded status, and shutdown is deterministic | Restrictions: Preserve existing local tools and simulated-trading authorization unchanged; do not rebuild the Agent session on each MCP state change; no TUI line overflow; app quit must await/dispose MCP safely before terminal shutdown | _Leverage: src/pi-agent.ts, src/app.ts, src/main.ts, src/commands.ts, Agent.setTools() | _Requirements: 1.4–1.5, 2.1–2.5, 3.3–3.6, 4.1–4.4 | Success: Integration tests show dynamic tool refresh, command output/status, local-tool continuity after MCP failure, and clean shutdown | Before starting mark this task [-] in tasks.md; after implementation log artifacts with spec-workflow log-implementation; then mark this task [x] in tasks.md._

- [x] 8. Add full extension workflow integration coverage
  - Files: `test/agent-extensions.test.ts`, `test/command-window.test.ts`, `test/main.test.ts`
  - Add deterministic end-to-end fixtures for project Skill discovery, `skill://` reads, `/skill`, stdio/HTTP/SSE MCP loading, command reload/reconnect, width fitting, failure isolation, and process teardown.
  - _Leverage: existing fake Agent drivers, `MemoryTerminal`, Bun test fixtures, prior unit tests_
  - _Requirements: All_
  - _Prompt: Implement the task for spec agent-skills-mcp, first run spec-workflow-guide to get the workflow guide then implement the task: Role: TUI integration QA engineer | Task: Add deterministic end-to-end coverage proving Skill and MCP behavior across real app command and lifecycle paths | Restrictions: Test observable behavior, not source text; never call real network services; fixture MCP servers must terminate reliably; retain all existing UI width assertions | _Leverage: test/main.test.ts MemoryTerminal, test/command-window.test.ts, Bun fixtures | _Requirements: All | Success: Tests prove no-extension compatibility, Skill discovery/read/invocation, each MCP transport, partial failure, reload/reconnect, clean shutdown, and TUI width invariants | Before starting mark this task [-] in tasks.md; after implementation log artifacts with spec-workflow log-implementation; then mark this task [x] in tasks.md._
